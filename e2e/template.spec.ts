// import { test, expect } from './helpers/shared-test.js';

// // Enforce serial execution so tests run sequentially on the shared page
// test.describe.configure({ mode: 'serial' });

// test.describe('Feature or Scenario Name', () => {
//   test('should perform action and verify result', async ({ sharedPage }) => {
//     // 1. Interact with the shared Foundry page
//     // await expect(sharedPage.locator('#interface')).toBeVisible();

//     // 2. Evaluate in Foundry game context
//     // const userName = await sharedPage.evaluate(() => {
//     //   const gameObj = (
//     //     window as unknown as { game?: { user?: { name?: string } } }
//     //   ).game;
//     //   return gameObj?.user?.name;
//     // });

//     // 3. Assert results
//     // expect(userName).toBe('Gamemaster');
//   });
// });
